import ProjectDetailPage from "@/features/projects/[id]/page";
import { generateStaticParams, generateMetadata } from "@/features/projects/[id]/page";

// Re-export metadata and static params generation
export { generateStaticParams, generateMetadata };

export default ProjectDetailPage;